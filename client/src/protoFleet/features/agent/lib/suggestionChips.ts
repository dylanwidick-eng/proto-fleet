/**
 * The six composer suggestion chips (proto-nav-chat.md §4.1). Chips SEED the
 * composer and focus it — they never auto-send. Via the ordered matchers,
 * "Compare site performance" and "Generate P&L report" intentionally hit the
 * fallback reply.
 */
export const SUGGESTION_CHIPS: string[] = [
  "Forecast failing hardware",
  "Recommend repairs",
  "Help schedule downtime",
  "Tune for optimal energy spend",
  "Compare site performance",
  "Generate P&L report",
];
